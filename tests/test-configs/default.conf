database {
  host = "localhost"
  port = 5432
  name = "testdb"
  credentials {
    username = "testuser"
    password = "testpass"
  }
}

server {
  host = "0.0.0.0"
  port = 8080
  timeout = 30
}

logging {
  level = "info"
  file = "/var/log/test.log"
  rotation {
    enabled = true
    maxSize = "10MB"
  }
}

features {
  authentication = true
  caching = true
}
