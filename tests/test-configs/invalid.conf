# Test case: Invalid HOCON syntax - missing closing brace

database {
  host = "test-invalid.example.com"
  port = 5432
  credentials {
    username = "invalid_user"
    password = "invalid_pass"
  # Missing closing brace here

server {
  host = "0.0.0.0"
  port = 8080
}
