# Test case: Missing multiple top-level and nested keys

database {
  host = "test-minimal.example.com"
  port = 5432
  # Missing name and credentials
}

server {
  port = 8080
  # Missing host and timeout
}

# Missing entire logging section
# Missing entire features section
