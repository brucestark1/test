# Test case: Missing some nested keys

database {
  host = "test-partial.example.com"
  port = 5432
  name = "partialdb"
  # Missing credentials section
}

server {
  host = "0.0.0.0"
  port = 8080
  # Missing timeout
}

logging {
  level = "warn"
  file = "/var/log/partial.log"
  # Missing rotation section
}

features {
  authentication = true
  caching = true
}
