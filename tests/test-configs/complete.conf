# Test case: All keys present - should pass with no missing keys

database {
  host = "test-complete.example.com"
  port = 5432
  name = "completedb"
  credentials {
    username = "complete_user"
    password = "complete_pass"
  }
}

server {
  host = "0.0.0.0"
  port = 9000
  timeout = 60
}

logging {
  level = "debug"
  file = "/var/log/complete.log"
  rotation {
    enabled = false
    maxSize = "20MB"
  }
}

features {
  authentication = true
  caching = false
}
